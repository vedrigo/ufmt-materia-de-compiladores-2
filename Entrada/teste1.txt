program id1
    var a : real;
    procedure id2 (b, c : integer; x : real)
        var a : integer
        begin
            while a = b do
                a := b;
                write(a)
            $
        end
    ;
    var c, b : integer
    begin
        a := a + 2.5;
        id2(b; b; a);
        if 1 <> c then
            a := 3.0
        $;
        write(a, b, c);
        read(b)
    end
.