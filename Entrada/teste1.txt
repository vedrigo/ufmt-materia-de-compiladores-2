program id1
    var a : real;
    procedure id2 (b : integer; x : real)
        var a : integer
        begin
            while a = b do
                a := b;
                write(a)
            $
        end
    ;
    var c : integer
    begin
        id2(a);
        if 1 <> c then
            a
        $
    end

.